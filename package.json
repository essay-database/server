{
  "name": "essaydb-server",
  "version": "1.0.0",
  "description": "essaydb server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 8080 --middlewares headers.js backup.js"
  },
  "dependencies": {
    "json-server": "^0.14.0",
    "uuid": "^3.3.2"
  }
}